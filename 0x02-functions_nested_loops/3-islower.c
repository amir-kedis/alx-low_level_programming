#include "main.h"

/**
 * _islower - tells if the given char is lower or not
 *
 * @c: the char that will be tested
 *
 * Return: 1 if c is lower, 0 otherwise
 */
int _islower(int c) { return (c >= 'a' && c <= 'z'); }
